# Copy to .env.dev / .env.qa / .env.uat and fill in.
# NOTE (Tom): these were never all committed. Sandra (SRE) has the real values.
BASE_URL=https://qa.brislane.example
API_URL=https://api-qa.brislane.example
AUTH_TOKEN=          # hardcoded bearer used to live here — do not do this
DB_URL=              # NOT DOCUMENTED per env
ENV=qa
